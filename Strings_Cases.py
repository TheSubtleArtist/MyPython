name = "aDa loVelace"
print(
	name.title())  # The title() method changes each word to title case, where each word begins with a capital letter.
print(name.upper())  # Forces upper case
print(
	name.lower())  # forces lower case. common to store strings as lower case and then determine display as the data is accessed for reading and display
